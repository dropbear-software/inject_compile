name: coffee
description: A coffee shop example using inject_compile
version: 1.0.0

environment:
  sdk: ^3.12.2
resolution: workspace

dependencies:
  inject_compile: ^0.9.0

dev_dependencies:
  inject_compile_generator: ^0.9.0
  build_runner: ^2.4.0
  lints: ^6.0.0
  test: ^1.25.6
