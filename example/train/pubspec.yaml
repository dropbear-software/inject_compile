name: train
description: A sample train application using inject_compile.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2
resolution: workspace

dependencies:
  inject_compile: ^0.9.0

dev_dependencies:
  build_runner: ^2.4.0
  inject_compile_generator: ^0.9.0
  test: ^1.25.6
